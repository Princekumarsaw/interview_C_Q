#include<stdio.h>
int main(){
	int principal=1000, rate=6, years=1;
	int simpleinterest = (principal * rate * years)/100;
	printf("the value of simple interest is %d",simpleinterest);
	return 0;
}  
